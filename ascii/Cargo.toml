[workspace]

[package]
name = "ascii"
version = "0.1.0"
edition = "2021"
description = "ASCII art post-process — luminance mapped to procedural character patterns."

[lib]
crate-type = ["cdylib"]

[dependencies]
renzora_plugin = { path = "../../sdk/plugin-api/renzora_plugin", default-features = false, features = ["libm"] }

[profile.dist]
inherits = "release"
opt-level = 2
strip = "symbols"
# no_std cannot unwind on stable.
panic = "abort"
