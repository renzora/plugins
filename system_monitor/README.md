# System Monitor

An FPS counter and hardware readout in the editor's status bar. Frame rate and frame time come from the same averaged measurement, so the pair always agree; the hardware query is throttled well below frame rate, because a status-bar readout does not need polling at 60 Hz.

**Where:** the right-hand end of the status bar. Nothing to add to a scene.

**Scope:** Editor.
