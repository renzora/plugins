# Invert

Inverts the colours of the frame. `intensity` crossfades, so a partial inversion is possible rather than only a full negative.

**Add it:** `+ Add Entity → Post Process → Invert`, or Add Component on an entity you already have.

**Settings:** `intensity`

**Shader:** `src/invert.wgsl` · **Scope:** Runtime (editor viewport and shipped game)
