# Gamepad

A debug panel that visualizes live controller input: both sticks, the triggers, and every button, per connected pad. Useful for confirming a controller is seen at all, checking deadzones, and telling two connected pads apart.

**Open it:** the **Gamepad** panel, under the "Input" group in the panel list.

**Scope:** Editor.
